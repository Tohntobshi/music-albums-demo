import { RootState } from ":types";
import { ActionCreators } from "../actions";

// export const reducer = (state: RootState, action: ReturnType<typeof ActionCreators.changeForm>): RootState => {
//   return state;
// };
